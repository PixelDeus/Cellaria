# Архитектура (внутреннее устройство)

Этот документ — **не история изменений** (для этого `git log`) и **не
модель** (для этого [specification.md](specification.md) — аксиомы,
формальная семантика, доказательства). Здесь — **текущее** устройство
реализации: как код внутри устроен и почему именно так, а не иначе.
Поддерживается в актуальном состоянии; при архитектурных изменениях
правится ЭТОТ файл.

---

## 1. Карта модулей

Каждый файл ниже 1000 строк — что не влезало, разбито по concern'ам
(несколько `impl`-блоков одного типа в разных файлах — валидно и
безопасно в Rust, публичный API не меняется от того, в каком файле лежит
метод).

```
src/
  lib.rs                — публичный API, реэкспорты, глобальный аллокатор
  types.rs               — Rule, Cell, ShiftSpec, ChangeValue и т.д.
  grid.rs                 — Grid<S> — обёртка над storage с унифицированным API
  storage.rs                — VecStorage (конечная) / ChunkStorage (бесконечная, чанки 64×64)
  error.rs                  — CellariaError, единый тип ошибки
  config.rs                 — загрузка YAML → (Grid, RuleIndex); тесты — config_tests.rs
  conflict_analyzer.rs        — статический граф конфликтов, RuleData/RuleDataCache
  rule_store.rs                — RuleStore, типы протокола; тесты — rule_store_tests.rs
  rule_store_deserialize.rs      — декодер протокола (find_terminator/deserialize_packet)
  rule_store_serialize.rs         — энкодер протокола (serialize_add_rule/...)
  tm_translator.rs                 — перевод машины Тьюринга в конфиг Cellaria
  render.rs                         — рендер решётки/стека слоёв (текст/JSON)
  fast_hash.rs                       — FxHash-алиасы (FxHashMap/FxHashSet)
  layered.rs                          — LayeredEngine<S>: стек 2D-слоёв, cross-layer чтение (см. §12)
  engine/
    mod.rs                             — Engine<S>/EngineSnapshot/InputEvent, объявления подмодулей
    lifecycle.rs                        — new/snapshot/from_snapshot/self-mod-конфиг/grid-доступ
    raw_phases.rs                        — "сырые" detect_matches/arbitrate/apply_matches (см. §2)
    io.rs                                 — push_input/pop_output/apply_input/drain_output/replay
    rules.rs                               — get_priority/find_rule/rule_index/rebuild_rule_cache
    state_mgmt.rs                           — advance_age/reset_age/detect_termination/compose_with
    tick.rs                                  — run_tick/run_tick_profiled/absorb_self_modifications
    pipeline.rs                               — run_tick_with_cache (сама механика пяти фаз тика)
    pipeline_search.rs                         — резолюция поисковых координат, ExtensionFlags-кэш
    matcher.rs                                  — Detect: поиск совпадений, CAM-поиск
    arbitrator.rs                                — Arbitrate: жадный арбитраж, spatial band-split
    applicator.rs                                 — Apply: запись изменений, recursion-каскад
    rule_state.rs                                  — RuleStateStore (см. §3)
  gpu/
    engine.rs                                       — GpuEngine: структуры, Pipeline enum
    engine_build.rs                                   — new/with_rounds/build_*_pipeline
    engine_tick.rs                                     — run_tick/dispatch_tick/cpu_fallback_resolve
    rule_table.rs                                       — GpuRuleTable, needs_arbitration, потолки
    rule_table_builder.rs                                — build_gpu_rule_table
    shader.wgsl                                           — compute-шейдеры (detect/arbitrate/apply)
```

---

## 2. Тик-пайплайн

`Engine::run_tick()` (`engine/tick.rs`) → `run_tick_with_cache()`
(`engine/pipeline.rs`), пять фаз:

1. **Input** — НЕ автоматическая часть тика. `push_input()` кладёт значение
   в очередь буфера; перенос очереди на граничную ячейку делает отдельный,
   явно вызываемый `apply_input()`. Канонический паттерн вызывающего кода:
   `apply_input()` каждый тик безусловно, ДО `run_tick()`.
2. **Detect** (`matcher.rs`) — поиск совпадений по всей решётке (или
   инкрементально по dirty-множеству), читает **только pre-tick** состояние
   решётки (дисциплина снимка тика, специф. §2.2.1). Параллелится через
   rayon выше `matcher::PARALLEL_THRESHOLD`.
3. **Arbitrate** (`arbitrator.rs`) — жадный алгоритм по детерминированному
   ключу сортировки (специф. §7); см. §5 ниже про пространственное
   разбиение на полосы.
4. **Apply** (`applicator.rs`) — запись изменений принятых матчей в буфер,
   один flush в решётку. `Rule::recursion` — единственное отступление от
   "detect читает только pre-tick": каскад читает уже накопленный этим же
   каскадом буфер (специф. §4.7).
5. **Flush** — единый проход `write_buffer` → `grid.set_cell()`, чистка
   `pattern_buffer`.

`spatial_bypass_split` (`engine/pipeline.rs`, вызывается КАЖДЫЙ тик) делит
совпадения на `safe` (арбитраж пропускается — см. специф. §10.7) и
`unsafe_matches` (идут в полный арбитраж) ДО фазы Arbitrate — это не
отдельная фаза, а предварительный фильтр внутри неё.

---

## 3. Персистентное состояние правил — `RuleStateStore`

`engine/rule_state.rs`. Единая точка хранения `starvation_counters`/
`feedback_counters`/`memory_buffers`/`activation_counters` +
`last_rebuilt_rule_index` (снимок для чистки осиротевших записей).

**Почему не четыре отдельных поля на `Engine` (как было раньше):**
дисциплина снимка тика (Detect/Arbitrate обязаны видеть эти карты КАК ОНИ
БЫЛИ на начало тика, запись — только после) раньше держалась ТОЛЬКО
соглашением программиста — простые `&mut FxHashMap` ничем не мешали
прочитать уже обновлённое в этом же тике значение. Именно так родился
реальный баг с `feedback_counters` (инкремент стоял МЕЖДУ чтением при
арбитраже и чтением при apply — см. CHANGELOG, `[0.7.0] / Fixed`).

**Как это закреплено в типах, а не в ревью:**
- `snapshot() -> RuleStateSnapshot<'_>` — держит `&RuleStateStore`
  (разделяемое заимствование), для Detect/Arbitrate.
- `mutate() -> RuleStateWriter<'_>` — держит `&mut RuleStateStore`
  (эксклюзивное), для пост-арбитражных обновлений.
- Borrow checker физически не даёт получить `mutate()`, пока где-то ещё жив
  `snapshot()` — ошибка компиляции, не баг в рантайме.

**Единственное осознанное исключение:** `applicator::apply_rule_buffered`
("Фаза 1") для ПОБЕДИВШЕГО матча с `Rule::feedback` — чтение счётчика (для
`feedback_override`) и его инкремент обязаны быть атомарной операцией ДО
релокации записи при сдвиге; разделять на снимок+писателя было бы опаснее
(легко забыть релоцировать после инкремента отдельным проходом). Единственное
место, где чтение идёт через `RuleStateWriter` целиком.

**Ключи состояний** (все в `arbitrator.rs`, рядом с типами):
| Состояние | Ключ | Почему |
|---|---|---|
| `StarvationCounters`, `FeedbackCounters`, `MemoryBuffers` | `(x, y, rule_idx)` | привязаны к позиции матча, переносятся при сдвиге (`apply_shift_buffered`) |
| `ActivationCounters` (`max_activations`) | `(head, rule_idx)` | ГЛОБАЛЬНЫЙ бюджет правила, не позиции — перенос не нужен |

---

## 4. Расширения модели: сводка

| Расширение | Поле `Rule`/`ShiftSpec` | Межтиковое состояние | CPU | GPU |
|---|---|---|---|---|
| CAM (притяжение к типу) | `Rule::cam` | нет (пересчитывается каждый тик) | да | да |
| Голодание | `starvation_after` | `StarvationCounters` | да | да |
| Каскад в тике | `recursion` | нет | да | да (`MAX_RECURSION_DEPTH=4`) |
| Защёлка направления | `feedback` | `FeedbackCounters` | да | да |
| FIFO-гейт по истории | `memory` | `MemoryBuffers` | да | да (оба триггера) |
| Излучение (не очищает источник) | `keep_source` | нет | да | да (кроме сочетания с `feedback`/`memory` — `FeedbackKeepSourceUnsupported`/`MemoryKeepSourceUnsupported`), см. `GpuMatch::keep_age_mask` в `shader.wgsl` |
| Глобальный бюджет побед | `max_activations` | `ActivationCounters` | да | **нет**, явно отвергается при построении GPU-таблицы (`rule_table.rs`'s `GpuUnsupportedReason` check) |
| Арифметика в changes | `ChangeValue::Add`/`Sub` | нет (чистая функция от pattern-буфера того же тика) | да | **нет** (`GpuUnsupportedReason::ChangeIsArithmetic`, та же категория, что `ChangeValue::Ref`) |

Комбинации, структурно исключённые (не "не реализовано", а нет
определения): `memory(RuleOutcome)` + `recursion` (у уровня каскада нет
отдельного шага арбитража — "выиграл/проиграл" не определено). Комбинации/
поля, исключённые только на GPU (реализуемы на CPU): `cam`+`recursion`,
`memory`+`recursion`, `memory`+`cam`, `feedback`/`memory`+`broadcast`,
`feedback`/`memory`+`changes` на том же смещении, что цель сдвига,
`feedback`/`memory`+`keep_source`, `max_activations` целиком (не в
сочетании с чем-то, а сам по себе), `ChangeValue::Add`/`Sub` целиком — см.
`gpu/rule_table.rs::GpuUnsupportedReason` и специф. §13.4 (таблица).

**`ChangeValue::Add`/`Sub` (2026-08-11)** — рекурсивная wrapping-u8
арифметика (`Add(a, b)`/`Sub(a, b)`, операнды сами `ChangeValue`). Не
кодируется протоколом самомодификации (`rule_store.rs`'s `push_change`
возвращает `Err`, не тихо пропускает — `changes` в отличие от
`feedback`/`memory`/`keep_source` УЖЕ активно кодируется поэлементно, так
что тихий пропуск конкретной записи означал бы тихую потерю данных, а не
"расширение недоступно целиком"). Честное структурное ограничение:
`CellType` — одновременно значение И ключ матчинга, так что "счётчик,
растущий каждый тик ОДНИМ правилом" не выражается — после первого
применения клетка становится другим `CellType`, исходное правило
(зарегистрированное под старым типом) больше не матчит. См. специф. §4.4
для полного описания.

**Заодно найден и исправлен реальный баг, не связанный с самой
арифметикой:** `ChangeValue` был `#[serde(untagged)]` — `Literal(u8)` и
`Ref(usize)` сериализуются НЕРАЗЛИЧИМО (оба как голое число), значит
`Ref(3)` после `Engine::snapshot()`/`from_snapshot()` (и `replay()`)
тихо возвращался как `Literal(3)` — ЛЮБОЕ правило с `Ref` в `changes`
молча теряло смысл после снимка. Ни один существующий тест этого не ловил
(ни один генератор property-тестов не строил `changes` с `Ref`). Убрано
`#[serde(untagged)]` — YAML-конфиги не затронуты (`config.rs`'s
`parse_change_value` парсит `changes` вручную, не через derive этого
enum).

**`Rule::cam` — почему радиус, а не поиск по всей решётке.** CAM ("магнит")
ищет ближайшую клетку `target_type` в диске Chebyshev-радиуса `radius`
вокруг головы и притягивает её значение — цель известна только в рантайме,
не на этапе определения правила, в отличие от обычного `pattern`/`shifts`.
Неограниченный (по всей решётке) поиск сломал бы статическую разрешимость
конфликтов — главное свойство, ради которого модель выбрала решётку, а не
граф (см. `paper2.md`). Radius-ограничение — прямое следствие этого
компромисса, не техническое ограничение реализации. Для графа конфликтов
(до просмотра содержимого решётки) affected-region правила с `cam` — весь
диск радиуса `radius` (то же консервативное огрубление, что уже
использует `arbitrate_spatial`'s `max_radius`); для арбитража конкретного
тика — уже точная найденная позиция, не весь диск. Правило с `cam` не
может иметь `shifts` (притяжение — единственный сдвиг правила). Доказано
и измерено в `examples/proof_cam_radius_search.rs` (совместимость с
моделью) и `examples/compare_cam_vs_wavefront.rs` (выигрыш по задержке
против того же эффекта, собранного обычными `shifts`-правилами волновым
поиском).

---

## 5. Пространственное разбиение арбитража

`arbitrate_spatial_with_cam` (`arbitrator.rs`) — при большом числе матчей
делит решётку на **полосы** по x-координате (столько полос, сколько rayon
потоков, но не уже удвоенного margin `2 × reach`). Каждая полоса —
`core` (совпадения дальше `margin` от ОБЕИХ границ полосы, арбитрируются
параллельно, Lemma 6 специф. — разные полосы никогда не делят клетки) +
`boundary` (совпадения ближе `margin` к какой-либо границе).

**Текущий алгоритм merge (после трёх реальных багов, найденных и
исправленных в этой области — полная история в `git log`):** каждая
полоса арбитрирует свой ПОЛНЫЙ core-набор целиком (core на
safe/at_risk ДО арбитража разделять нельзя — тогда два core-матча одной
полосы рядом с границей перестают сравниваться друг с другом). Из уже
принятых результатов:
- `safe_margin = margin + 2*reach` (не `margin + reach` — та формула
  недооценивала худший случай и была реальным багом в уже выпущенном
  коде, найдена только адверсариальным тестом на правиле с СИММЕТРИЧНЫМ
  affected-регионом, то есть с клетками по ОБЕ стороны от центра; обычный
  однонаправленный сдвиг физически не может проявить эту ошибку, поэтому
  предыдущие тесты её не ловили): матчи с `dist >= safe_margin` от ОБЕИХ
  границ доказанно недостижимы никаким boundary-матчем — не как ЛИНИЯ
  (той проверки недостаточно), а как множество КОНКРЕТНЫХ клеток: худший
  boundary дотягивается до `dist=margin-1+reach`, худший safe-матч на
  самой границе `safe_margin` имеет клетки от `safe_margin-reach` —
  диапазоны разделены ровно при `safe_margin = margin+2*reach`, не при
  `margin+reach`. Финальны, без дальнейших проверок.
- Остальные — не только победители, а ИСХОДНЫЕ кандидаты at-risk зоны
  целиком (`at_risk_zone_by_band`, и победители, и локально отвергнутые,
  собранные на этапе классификации) — идут ОДНИМ дополнительным проходом
  `arbitrate_with_cam` вместе с `boundary`, тем же детерминированным
  тотальным порядком, что и централизованный арбитраж. Локально отвергнутые
  нужны в этом проходе, а не только победители полосы: если сильный
  локальный победитель проигрывает boundary-конкуренту, локальный
  проигравший обязан получить честный шанс на освободившуюся клетку — то
  же самое даёт централизованный арбитраж.
- `safe_accepted`'s клетки передаются в этот merge-проход как
  предзарезервированные (`reserved: &[(i32, i32)]`, `&[]` — нулевая цена
  для всех остальных вызовов) — at-risk зона может физически пересекаться
  с safe-зоной той же полосы, без резервации локально-отвергнутый
  кандидат мог бы повторно "выиграть" клетку, уже навсегда занятую
  `safe_accepted`.

Геометрически доказано (конструктивно, любой `reach >= 1`): ни один
конечный margin с простой пороговой схемой (`dist >= margin ⟹ core`) не
может гарантировать отсутствие пересечения boundary-записи с соседней
core-зоной без этого merge-прохода — отсюда его необходимость, не
перестраховка. Проверено систематически на всех расширениях модели
(`recursion`, `cam`, обычный сдвиг, `feedback` — по одному repro-тесту на
класс вычисления affected-региона; `starvation_after`/`memory`/
`max_activations` не меняют вычисление affected-региона вообще, не новый
геометрический риск). Сабораж-верифицировано на каждом фиксе. Первая
(наивная) версия мержа сливала ВСЕ core-матчи обратно в дополнительный
проход — корректно, но измеренная регрессия ~30-50% на CA-пробке;
текущая (targeted) версия — в пределах шума от исходной производительности.

**Известная асимметрия по-прежнему НЕ исправлена (осталась как была):** на
регулярной (периодической) нагрузке (например, сплошной CA-сдвиг) boundary
— не пропорциональная доля работы: ~32% матчей, но 60-74% совокупного
времени core+boundary (измерено на CA-пробке 300×300, 24 полосы). Причина
— boundary последовательный, core параллельный на N потоках. Эта проблема
и попытка её решить (рекурсивное деление boundary) — ОТДЕЛЬНЫЙ вопрос от
бага выше, не связаны напрямую.

**Почему не исправлено рекурсивным делением boundary** (пробовали дважды):
рекурсивное применение того же `arbitrate_spatial_with_cam` к `boundary`
математически корректно по той же Lemma 6, но **не сходится** на
регулярных паттернах — border-границы каждого следующего уровня рекурсии
считаются той же формулой (`spread/num_bands`), и на равномерно заполненной
решётке новый boundary почти точно ложится на позиции границ следующего
уровня (резонанс периода паттерна с периодом band-split) — множество не
уменьшается, реальный прогон дал **stack overflow**. Депт-ограниченная
версия с jitter'ом (чередование числа полос по чётности уровня рекурсии)
terminates безопасно и корректна (полный suite зелёный), но контролируемый
A/B реального выигрыша не дал (arbitrate не изменился, apply даже чуть
хуже — лишний rayon-диспетчинг/аллокации на уровень рекурсии стоят больше,
чем экономит jitter). Обе попытки полностью откачены.

---

## 6. Горячий путь: персистентность и аллокации

Общий паттерн, найденный и исправленный несколько раз за разные буферы:
**свежий `Vec`/`HashMap` на каждый вызов горячей функции** — там, где число
вызовов за тик велико (по одному на матч или на полосу), стоимость
аллокации доминирует над полезной работой.

- **`Engine`-персистентные поля** (однопоточный вызывающий код):
  `write_buffer` (`applicator.rs`), `pattern_buffer` — `.clear()` вместо
  `Vec::new()`/`HashMap::new()` на каждый вызов, передаются как `&mut`.
- **`thread_local!`-персистентные буферы** (когда владеющая функция
  вызывается ПАРАЛЛЕЛЬНО через rayon — `Engine`-поле сюда физически не
  подходит, один буфер не может быть общим mutable-состоянием нескольких
  потоков одновременно): `USED_CELLS_BUF`/`AFFECTED_BUF`/`KEYED_BUF` в
  `arbitrate_with_cam` (`arbitrator.rs`) — каждый rayon-воркер держит свой
  персистентный буфер, переживающий вызовы между полосами И между тиками на
  этом же потоке (thread pool долгоживущий).
  - `.clear()` в начале каждого использования буфера **не всегда
    load-bearing** — например, для `KEYED_BUF` он доказуемо избыточен
    (тот же `.drain(..)` в конце функции уже безусловно опустошает буфер к
    следующему вызову), но оставлен как defensive-практика. Для
    `USED_CELLS_BUF`/`AFFECTED_BUF` он строго обязателен (диверсия ломает
    20 тестов) — буфер не дренируется, только читается/пишется по ключу.
  - **Заимствование `KEYED_BUF` — только на мгновенный `take`/возврат**
    (`std::mem::take`), сама сортировка (`par_sort_unstable_by`) идёт на
    локальной переменной, БЕЗ удержания `RefCell`-заимствования на всё
    время сортировки. Иначе: rayon'овский work-stealing может увести тот
    же OS-поток на ДРУГОЙ вызов `arbitrate_with_cam` (соседняя полоса,
    работающая с тем же thread-local) пока первый вызов ещё не вернулся
    из сортировки (ждёт свои подзадачи через `join`) — второй вызов
    пытается занять тот же `RefCell` и падает с "already borrowed"
    (реальный найденный баг, срабатывающий только при per-полосном размере
    `>= PARALLEL_SORT_THRESHOLD=1024`, см. CHANGELOG). Регрессионный тест:
    `tests/property_arbitration.rs::test_arbitrate_spatial_no_refcell_reentrancy_panic_with_many_matches_per_band`.
  - `USED_CELLS_BUF`/`AFFECTED_BUF` этому риску НЕ подвержены — их область
    заимствования не содержит вложенных rayon-вызовов (проверено через
    `grep` по `par_iter`/`par_sort`/rayon:: в `arbitrator.rs`).
- **`HeadRuleIndex`** (`[Option<&Vec<Rule>>; 256]`, `engine/pipeline.rs`) —
  замена `rule_index.get(&head)` там, где вызывается на каждый матч.
  `CellType` оборачивает `u8` (256 значений) — хэширование не нужно
  вообще, прямая индексация. Строится один раз ЗА ВЫЗОВ горячей функции
  (`build_head_index`), НЕ персистентное поле `Engine` — заимствует у
  `rule_index`-параметра, персистентность потребовала бы самоссылающейся
  структуры (сознательно избегается во всём проекте, без `unsafe`/
  `ouroboros`).
  - **`RuleDataCache`** (`conflict_analyzer.rs`) — та же проблема
    хэширования, была `HashMap<(CellType, usize), RuleData>`, ТЕПЕРЬ тоже
    `[Option<Vec<RuleData>>; 256]` (закрытый follow-up). В отличие от
    `HeadRuleIndex` — не эфемерная, а ВЛАДЕЮЩАЯ структура, персистентное
    поле `Engine::rule_cache`, перестраивается только при смене состава
    правил (`rebuild_rule_cache`), не каждый тик — самоссылающейся
    структуры здесь нет, `build_rule_data_cache` просто строит владеющий
    массив заново. Измерено на CA-пробке поверх уже применённого
    `HeadRuleIndex`+`mimalloc`: arbitrate/apply ещё немного ниже
    (~10-12мс/13-15.5мс → ~8-11.5мс/10-12.4мс).
- **Глобальный аллокатор** — `mimalloc` (`lib.rs`, `#[global_allocator]`),
  не системный. После всех точечных персистентных буферов выше тик всё
  ещё заметно упирается в malloc/free как таковой (~30-40% на всех трёх
  фазах тика на CA-пробке) — многие оставшиеся аллокации (например,
  `AffectedRegion::written_cells`, по одному на матч) не устранимы простым
  переиспользованием контейнера без нарушения публичного API
  (`apply_matches` должен возвращать `Vec<AffectedRegion>` по значению).
  Смена аллокатора забирает бОльшую часть выигрыша, ради которого
  рассматривалась bump-арена на весь тик, почти без инвазивности.
  За фичей `mimalloc-alloc` (default-on, `Cargo.toml`) — `#[global_allocator]`
  объявляется РОВНО ОДИН РАЗ на весь финальный бинарник; `cellaria` не
  только `main.rs`, но и библиотека с публичным API, значит потребитель
  `cellaria`-как-зависимости, желающий свой аллокатор, иначе получил бы
  неразрешимый со своей стороны конфликт компиляции. `default-features =
  false` у потребителя убирает декларацию целиком.

**Не сделано, но исследовано (с оговоркой почему):**
- **SoA-раскладка решётки** (`Cell{value,born_at}` → раздельные массивы) —
  реальный выигрыш (-25.7% на чтении соседей в detect), но требует
  переписать `GridStorage`, обе реализации и весь публичный API доступа к
  клетке. Не оправдано текущим масштабом узкого места.
- **GPU+CPU async-пайплайнинг тиков** (перекрытие GPU-расчёта тика N+1 с
  CPU `cpu_fallback_resolve` тика N) — архитектурная задача сравнимого
  масштаба с §5 или крупнее, не мелкая правка. Открытый вопрос, решение не
  принято.

---

## 7. Статический анализ конфликтов и пространственный bypass

`conflict_analyzer::ConflictGraph` (специф. §10) — статический граф "какие
головы структурно МОГУТ конфликтовать", строится один раз при
создании/перестройке `Engine`. Два независимых потребителя:

1. **Загрузка конфига** — `analyze_conflicts()`, отчёт для пользователя
   (`main.rs` печатает "арбитраж не нужен" или список конфликтующих пар).
2. **Рантайм, каждый тик** — `Engine::conflict_partners` +
   `Engine::max_affected_radius` → `spatial_bypass_split` (см. §2 выше):
   совпадения без структурных партнёров ИЛИ без партнёров в радиусе
   `2 × max_affected_radius` от себя идут в `safe` без арбитража вообще.

**Ограничение графа, НЕ являющееся живой дырой** (проверено конкретно,
2026-08-10, не только рассуждением): `ConflictGraph` доказывает только
**write-write** непересечение (`affected_regions_overlap` сравнивает
`write_cells` только с `write_cells`), **не write-read**. На первый взгляд
это значит, что safe-матч мог бы писать туда, откуда ДРУГОЙ матч читает
через `$N`/`ChangeValue::Ref`. **На практике это НЕ достижимо при текущей
архитектуре**: `pattern_buffer` (источник значений для `$N`, заполняется в
`apply_rule_buffered`, `applicator.rs`) читает ИСКЛЮЧИТЕЛЬНО через
`grid.get_cell()` — саму решётку, а не `write_buffer`. `grid` физически не
мутируется НИГДЕ внутри цикла по матчам (`apply_matches_with_cam`,
`applicator.rs`) — единственная точка записи в `grid` за весь тик,
`write_buffer.drain()` → `grid.set_cell()` (та же функция, flush-фаза в
конце), выполняется ОДИН раз, ПОСЛЕ того, как все матчи (оба бакета — safe
И arbitrated) уже дописали свои изменения в буфер. Значит `$N` у ЛЮБОГО
матча в этом тике физически не может увидеть ничей чужой write — решётка
для всех одинаково pre-tick вплоть до самого flush, независимо от порядка
обработки матчей и независимо от того, что доказывает граф конфликтов.

Ограничение графа реально работает не как живая уязвимость, а как
**обоснование, почему прямая запись в `grid` для safe-ветки — вместо
буфера — не рассматривается**: именно это было причиной отказа от "пути
(в)" (см. §6, VecSink) — прямая запись СДЕЛАЛА БЫ write-read риск реальным
(нарушила бы только что описанную гарантию), а буферизация в её нынешнем
виде делает риск структурно недостижимым. Если когда-либо в будущем кто-то
предложит писать напрямую в `grid` для оптимизации safe-ветки (правдоподобная
идея, уже предлагалась) — вот тогда граф придётся расширять до write-read,
не раньше.

---

## 8. GPU-бэкенд

`gpu/engine.rs` — `GpuEngine`; `dispatch_tick` (`gpu/engine_tick.rs`) полностью синхронный (detect
+ `rounds` раундов арбитража/apply в одном submission, один
`device.poll(Maintain::Wait)` за тик — нет перекрытия с CPU, см. §6 про
нереализованный async-пайплайнинг).

**Два пайплайна** (специф. §13.1), выбираются автоматически по составу
правил конфига — не только "статичные правила без сдвигов": v2 поддерживает
сдвиги и запись в соседа через раундовый lock-free арбитраж
(`GpuRuleTable::needs_arbitration`).

**Гибридный fallback** (специф. §13.2) — длинные конфликтные цепочки,
не сошедшиеся за `rounds` раундов на GPU, доигрываются на CPU
(`cpu_fallback_resolve`); после — GPU-side `match_state` буфер обязан быть
обновлён финальным ACCEPTED/REJECTED (исторический баг — доигранные на CPU
матчи навсегда оставались PENDING(0) для `update_starvation_pass`).

**Персистентные буферы для межтикового состояния** (специф. §13.3) —
`starvation_after`/`feedback`/`memory` хранят состояние в
storage-буферах, которые НИКОГДА не очищаются между тиками (в отличие от
большинства GPU-буферов, живущих один тик) — тот же принцип, что
`thread_local!`/`Engine`-персистентность на CPU (§6), но на уровне
GPU-памяти, а не Rust-памяти.

**Подмножество, которого GPU НЕ поддерживает** — `gpu/rule_table.rs`,
`GpuUnsupportedReason` (см. таблицу §4 выше и специф. §13.4).

**Пределы подмножества (константы `rule_table.rs`) — почему именно эти
числа, не техническая случайность:**

- `MAX_SHIFT_REACH=12`/`MAX_CHANGE_REACH=4` — потолки |dx|/|dy| обычного
  сдвига/`changes`-смещения.
- `MAX_BROADCAST_REACH=4`, ОТДЕЛЬНЫЙ (более узкий) потолок именно для
  `broadcast: true` — broadcast пишет ВСЕ промежуточные клетки пути, не
  только конечную точку, значит число ячеек записи растёт ЛИНЕЙНО со
  `steps`, а не константно (=1), как у обычного сдвига.
  [`MAX_WRITE_CELLS`] — compile-time размер массива `GpuMatch::cells` в
  шейдере (WGSL требует константный размер поля структуры), ОБЩИЙ для
  ВСЕХ матчей ВСЕХ конфигов сразу (шейдер компилируется один раз
  статически) — если бы broadcast использовал общий потолок 12, это дало
  бы `MAX_WRITE_CELLS`=33 (рост в 3×) ради свойства, которым пользуется
  меньшинство конфигов; 4 даёт 17 (рост 55%).
- `MAX_CAM_RADIUS=16` — GPU (в отличие от CPU, `radius: u8` до 255 без
  потолка) сканирует диск `(2R+1)²` НА КАЖДУЮ клетку-кандидата каждый тик
  И раздувает дополненную сетку арбитража на `2×radius` по каждой оси —
  оба растут квадратично/линейно с R, нужен реальный потолок.
- `MAX_RECURSION_DEPTH=4` — `recursion` GPU-совместим (в отличие от
  `feedback`/`memory`/`starvation_after`, требующих межтиковое
  CPU-состояние) именно потому, что каждый уровень каскада читает только
  клетки, УЖЕ ЗАПИСАННЫЕ этим же каскадом (по построению — `recursion`
  требует пустые `shifts`, конфликтующий чужой матч был бы исключён
  арбитражем ДО начала каскада) — чисто локальное вычисление одного
  потока, без межпоточной синхронизации.
- `MAX_MEMORY_WINDOW=4` — FIFO-буфер памяти в storage — плоский
  `array<atomic<u32>>`, индексируемый `m * MAX_MEMORY_WINDOW + i` (прямая
  top-level индексация, не поле-массив внутри значения — тот случай,
  который naga действительно запрещает), длина ограничена только явным
  потолком, не техническим ограничением WGSL.
- `MAX_MARGIN = MAX_SHIFT_REACH + MAX_CHANGE_REACH` — CPU'шный
  `arbitrator::get_match_affected_cells` при `OverflowAction::Discard` НЕ
  клэмпит уходящие за границу решётки относительные ячейки (клэмпинг
  только для `Write`/`WriteLiteral`) — значит `arbitrate()` учитывает
  конфликт даже между двумя матчами, чьи "цели" совпадают ЗА пределами
  решётки. Найдено экспериментально (`tests/gpu_v2_correctness.rs`): без
  этого запаса GPU/CPU расходились у самого края решётки.
- Общий паттерн для всех потолков (`MAX_ID_BYTES=8`,
  `MAX_MATCHES_PER_CELL=8` и т.д.): скромное, но достаточное для
  подавляющего большинства реальных конфигов число — превышение даёт
  явный отказ (`GpuUnsupportedReason`), не молчаливое усечение.

---

## 9. Самомодификация (`RuleStore`)

`rule_store.rs` — wire-протокол для добавления/удаления правил во время
работы движка. Формат пакета — байт-ориентированный, с историческими
ограничениями формата (см. doc-комментарии констант `TERMINATOR`/маркеров
опкодов в файле — например, priority 240 физически невозможно закодировать
в обычном `AddRule`, приоритет 255 недостижим вообще). Расширенный формат
`AddRuleExtended` (маркер `0xF2`) кодирует `cam`+`recursion`; `feedback`/
`memory`/`keep_source` протоколом НЕ кодируются вообще (сериализатор
возвращает `Err`, не портит данные тихо) — единственные три расширения
модели, недостижимые через самомодификацию.

`Engine::enable_self_modification()` / `enable_guarded_self_modification()`
— второй режим ограничивает самомодификацию head-id владением (не любой
матч может менять любую голову) — специф. §12.

---

## 10. Сохранение и воспроизведение состояния

`Engine::snapshot()`/`from_snapshot()` — полное состояние (`grid`,
`rule_index`, весь `RuleStateStore`, self-mod состояние) через
`EngineSnapshot<S>`. Сериализация — **`serde_yaml`, не `serde_json`**: у
модели данных нестроковые ключи `HashMap` почти всюду (`(x,y)`,
`(head, rule_idx)` и т.п.), которые `serde_json` не умеет сериализовать в
принципе. Единственное исключение — `TickLogEntry` (плоская структура без
нестроковых ключей), которая использует `serde_json` штатно.

`Engine::replay(snapshot, log, target_tick)` — восстановление +
проигрывание журнала входных событий вперёд. Обязан вызывать
`apply_input()` на каждом воспроизводимом тике сам (см. §2 — Input не
автоматический).

---

## 11. Открытые архитектурные вопросы

Все пункты, поднятые предыдущей версией этого списка, разобраны и закрыты
(последнее обновление 2026-08-11) — либо реализацией, либо намеренным
окончательным решением НЕ делать, с обоснованием, либо (пункт про
остаточный риск в boundary-vs-core) отдельным целенаправленным тестом,
подтвердившим, что риск был реальным, и фиксом. Ничего не осталось "просто
открытым" без вердикта.

**1. Рекурсивное деление boundary-полосы (§5) — закрыто, НЕ делать.**
Испробовано ДВА разных подхода:
- Наивная безусловная рекурсия — ломается на регулярных (периодических)
  паттернах (резонанс периода паттерна с периодом band-split), реальный
  прогон дал stack overflow.
- Депт-ограниченная (`MAX_BAND_SPLIT_DEPTH=4`) версия с jitter (чередование
  числа полос по чётности уровня рекурсии, чтобы сбить резонанс) —
  терминацию гарантирует, корректность подтверждена полным suite (включая
  property-тесты spatial-vs-централизованный арбитраж), но контролируемый
  A/B на CA-пробке реального выигрыша не показал: arbitrate практически не
  изменился, apply даже немного хуже — лишний rayon-диспетчинг и
  Vec-аллокации на каждом уровне рекурсии стоят больше, чем экономит
  jitter. Обе попытки полностью откачены; код `arbitrator.rs` вернулся к
  простому нерекурсивному boundary. Другой алгоритм (не рекурсия по той же
  функции) потребовался бы для реального выигрыша, если он вообще
  существует — не переоткрывать эти два конкретных подхода без новых
  данных.

**2. Протокол самомодификации: приоритеты 240/241/242 — закрыто, ОКАЗАЛОСЬ
УЖЕ РАБОТАЕТ.** Изначальная формулировка этого пункта ("зарезервированные
приоритеты 240/255, фикс ломает формат") была НЕТОЧНОЙ — перепроверка кода
показала, что `serialize_add_rule`'s `needs_ext` уже автоматически
переключается на формат `AddRuleExtended`, когда `priority` совпадает с
`OP_REMOVE`/`OP_CLEAR`/`OP_ADD_EXT` (240/241/242) — приоритет там
передаётся ОТДЕЛЬНЫМ байтом, не как дискриминатор пакета, коллизии нет.
Уже покрыто существующим тестом
(`test_serializer_auto_switches_to_extended_format_for_reserved_priorities`
в `rule_store_tests.rs`) — полный round-trip сериализация→десериализация,
подтверждённый для всех трёх значений. Ничего чинить не нужно было.
**Приоритет 255 (0xFF) остаётся недостижим** — но это ГЛУБЖЕ, чем проблема
приоритета: `find_terminator` сканирует ПЕРВЫЙ байт `0xFF` в сыром потоке
как границу пакета ДО разбора, значит НИКАКОЙ байт полезной нагрузки
(priority, id, шаг сдвига, dx/dy/value) не может равняться 255 — не только
priority. Фикс потребовал бы byte-stuffing/escaping через весь формат, не
точечной правки — оставлено как есть, задокументировано в
`rule_store.rs`'s doc-комментариях у `OP_REMOVE`, не оправдано без
конкретной нужды.

**3. GPU+CPU async tick pipelining (§6, §8) — закрыто, решено НЕ делать
сейчас.** Реальный потенциал для arbitrate-доминантных (CA-подобных)
сценариев (перекрытие GPU-расчёта тика N+1 с CPU `cpu_fallback_resolve`
тика N), но требует прототипа (двойная буферизация состояния решётки,
синхронизация async GPU submission с CPU fallback) сравнимой сложности с
самым рискованным экспериментом этой сессии (VecSink, который не
окупился). Осознанное решение: не начинать без отдельного дизайн-прохода и
конкретной нужды, не наугад в рамках той же сессии, что уже дважды
проверяла похожие гипотезы на прочность и обе не подтвердила.

**4. SoA-раскладка решётки (§6) — закрыто, решено НЕ делать.**
`Cell{value,born_at}` → раздельные массивы — реальный, измеренный выигрыш
(-25.7% на изолированном чтении соседей в detect), но требует переписать
`GridStorage`, обе реализации (`VecStorage`/`ChunkStorage`) и весь
публичный API доступа к клетке (`grid.get_cell() -> Option<&Cell>`
физически не может остаться таким при раздельных массивах). Рассмотрена
ОДНА более дешёвая альтернатива (сузить `born_at: u64` до `u32`, вдвое
меньше сама структура `Cell`, без переписывания `GridStorage`) — отклонена
без реализации: тоже breaking (`pub born_at`), нулевой измеренный выигрыш
(идея, не проверенная гипотеза), и вносит риск молчаливого переполнения
после ~4 млрд тиков непрерывной работы. Не оправдано без реального
измерения масштаба выигрыша.

Закрытые ранее пункты (были в этом списке, реализованы — не переоткрывать):
`rule_cache: RuleDataCache` переведён на массив (§6, тот же приём, что
`HeadRuleIndex`); `#[global_allocator]` вынесен за default-on фичу
`mimalloc-alloc` (§6) — не ломает потребителей `cellaria`-как-библиотеки;
остаточный теоретический риск в исправленном boundary-vs-core (§5, бывший
пункт 5) — БЫЛ реальным (не гипотетическим), закрыт 2026-08-11 через
целенаправленно сконструированный тест ровно по описанному здесь рецепту
(три конкурента на клетку) плюс фикс `at_risk_zone_by_band`/`reserved` —
подробности в §5.

---

## 12. Многослойная решётка (`LayeredEngine`)

Стек ОДИНАКОВЫХ по размеру 2D-`Engine`, не настоящий 3D (см.
`layered.rs`'s doc-комментарий модуля) — выбор одновременно инженерный
(не переписывать `Grid`/`GridStorage`/matcher/conflict_analyzer/arbitrator/
GPU, которые все предполагают 2D) и семантический (слои "города" — это
ДОМЕНЫ: дороги/здания/коммуникации/сенсоры, а не геометрическая высота,
которую настоящий 3D бы смешал с ними). Реализация целиком аддитивная —
`Grid`/арбитраж НЕ тронуты ни единой строкой; `Engine` получил один новый
`pub(crate)`-метод (`run_tick_with_cross_layer_filter`, см. ниже) и один
новый опциональный параметр `run_tick_with_cache`, нулевые накладные
расходы для всех остальных вызывающих (`None`).

**Модель.** `Rule.cross_layer_reads: Vec<(dx, dy, dz, CellType)>` —
отдельное от `pattern` поле (не unified, чтобы не трогать существующие
250+ мест постройки `Rule`). `dz` индексирует СОСЕДНИЙ слой относительно
слоя, на котором правило проверяется, не координату. Первый срез — только
ЧТЕНИЕ: запись через слои (`changes`/`shifts` с `dz != 0`) не
поддерживается, отдельное расширение. Из этого следует ключевой инвариант:
слои БЕЗ `cross_layer_reads` между ними структурно НИКОГДА не конфликтуют
по записи (запись остаётся в своём слое) — межслойный арбитраж не нужен
вообще, не для конкретного набора правил, а по построению.

**`LayeredEngine<S>`** (`src/layered.rs`) — `Vec<Engine<S>>`, каждый слой
полноценный, независимый `Engine` (свой кэш, своё персистентное состояние
правил), но ВСЕ слои используют ОДИН И ТОТ ЖЕ `rule_index` (клонируется на
каждый слой при постройке — правило не привязано к конкретному слою, `dz`
сам определяет цель). `run_tick`:

1. Быстрый путь: если НИ ОДНО правило нигде в стеке не использует
   `cross_layer_reads`, слои структурно независимы (см. модельный
   инвариант выше) — каждый слой тикает обычным `Engine::run_tick()`,
   нулевые накладные расходы.
2. Иначе — решётки ВСЕХ слоёв клонируются ОДИН РАЗ, до того как хоть один
   слой применит свой тик (даёт разом и дисциплину снимка тика §2.2.1,
   распространённую через границу слоя — сосед читается таким, каким он
   был на начало ЭТОГО тика, даже если сам поменяется позже в том же тике,
   и снимает конфликт заимствования между `&self` внутри фильтра и
   `&mut self.layers[i]` снаружи, поскольку фильтр захватывает только
   собственные локальные данные). На каждом слое строится замыкание-фильтр
   по этому предтиковому снимку (`dz` за пределы стека или отрицательная
   итоговая координата — жёсткий отказ условия, симметрично тому, как
   `pattern` читает за пределами решётки, но здесь именно отсутствие слоя,
   не просто пустая клетка) и передаётся в
   `Engine::run_tick_with_cross_layer_filter` — `pub(crate)`-метод,
   идентичный `Engine::run_tick()`, но с доп. параметром
   `cross_layer_filter: Option<&dyn Fn(&RuleMatch) -> bool>`, применяемым
   внутри `run_tick_with_cache` сразу после слияния `cam`-совпадений и ДО
   гейтов `memory`/`max_activations` и учёта `starvation`/`feedback` —
   отфильтрованный матч для всего нижестоящего пайплайна выглядит так,
   будто паттерн вообще не совпал.

**Найденный и исправленный баг (см. CHANGELOG для полной истории):** до
этого фикса `run_tick` был собран вручную через `Engine::detect_matches()`
+ свободные `arbitrate()`/`apply_matches()` — "raw"-методы, ни один из
которых не хранит состояние между вызовами. Из-за этого `cam`,
`starvation_after`, `feedback`, `memory` и `max_activations` были тихим
no-op на ЛЮБОМ `LayeredEngine`, для любого слоя и правила — не
"непротестированная комбинация", а порча гарантии. Раз слои никогда не
делят клетки по записи (см. модельный инвариант выше), арбитрировать
МЕЖДУ слоями по-прежнему нечего — каждый слой арбитрирует СВОЙ тик
самостоятельно через полный `Engine`-пайплайн, что и требовалось.

`LayeredEngine::new` проверяет (`assert!`) все слои на одинаковый размер —
без этой проверки координата, валидная на одном слое, могла бы молча
указывать на другую клетку (или ни на какую) на другом слое внутри
фильтра, без единой ошибки ни при постройке, ни во время тика.

**Персистентность.** `LayeredEngine::snapshot()`/`from_snapshot()`
(`LayeredSnapshot<S>`) — по одному `EngineSnapshot` на слой, та же
`serde_yaml`-дисциплина §10 (не `serde_json` — те же нестроковые ключи
`HashMap` внутри каждого `EngineSnapshot`). Общий `rule_index` отдельно не
хранится — он уже часть КАЖДОГО `EngineSnapshot`, ровно как если бы слои
снимались по отдельности.

**Конфигурация.** `config::load_layered_config(paths: &[&str])` — грузит
`LayeredEngine` из нескольких YAML-файлов (по одному на слой), каждый ровно
тот же формат, что обычный `load_config` — отдельного "многослойного"
YAML-формата не вводится. Правила ВСЕХ файлов сливаются в один общий
`rule_index` (см. модельный инвариант выше) и пересортированные заново по
приоритету после слияния (конкатенация уже отсортированных per-файл
кусков не остаётся отсортированной сама по себе). Разноразмерные слои —
`CellariaError::GridBounds` с понятным сообщением, а не паника
`LayeredEngine::new` (внешние данные, не доверенный программный
вызывающий код).

**Сознательно не сделано в этом срезе** (осознанное сужение области при
изначальном дизайне): запись через слои, GPU-поддержка `LayeredEngine` —
оба отдельные, более сложные расширения, не блокируют текущий срез
read-only cross-layer чтения.

**Производительность (замерено, см. CHANGELOG для точных цифр):** при
реалистичной разрежённости (~4% активных клеток, городской сценарий)
`LayeredEngine` наследует то же ~40x преимущество dirty-tracking, что и
одиночный `Engine`, — обёртка сама по себе не вносит заметной деградации.
Плотный худший случай растёт линейно от числа слоёв (ожидаемо — слои
независимы, общей работы между ними нет), что делает CPU-only характер
`LayeredEngine` (GPU не поддерживается) реальным, а не только
гипотетическим ограничением при большом числе плотно-активных слоёв —
не критично для реалистичной разрежённости, стоит держать в уме при росте
числа доменов/слоёв.
